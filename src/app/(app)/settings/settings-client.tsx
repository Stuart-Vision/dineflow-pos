'use client';
import { Bell,Building2,Gift,Receipt,Save } from 'lucide-react';import * as React from 'react';import { toast } from 'sonner';import { PageHeader } from '@/components/shared/page-header';import { useSessionUser } from '@/components/providers/session-provider';import { Button } from '@/components/ui/button';import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card';import { Input,Textarea } from '@/components/ui/input';import { Label } from '@/components/ui/label';import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';import { Switch } from '@/components/ui/switch';import { Tabs,TabsContent,TabsList,TabsTrigger } from '@/components/ui/tabs';import { PERMISSIONS } from '@/constants/permissions';import { apiGet,apiPatch,ApiRequestError } from '@/lib/api/client';
interface Data{restaurant:{name:string;legalName?:string;defaultCurrency:string;timezone:string;dateFormat:string};settings:{receiptHeaderText:string;receiptFooterText:string;showLogoOnReceipt:boolean;defaultReceiptPaperSize:string;invoiceNumberPrefix:string;orderNumberPrefix:string;taxMode:string;serviceChargePercent:number;cashRoundingIncrementMinor:number;loyaltyPointsPerCurrencyUnit:number;loyaltyRedemptionRateMinorPerPoint:number;notifyLowStock:boolean;notifyReservationReminders:boolean;themePrimaryColor:string}}
export function SettingsClient(){const user=useSessionUser(),manage=user.permissions.includes(PERMISSIONS.SETTINGS_MANAGE);const[data,setData]=React.useState<Data|null>(null),[saving,setSaving]=React.useState(false);React.useEffect(()=>{void apiGet<Data>('/api/settings').then(setData)},[]);async function save(){if(!data)return;setSaving(true);try{setData(await apiPatch<Data>('/api/settings',data));toast.success('Restaurant settings saved')}catch(e){toast.error(e instanceof ApiRequestError?e.message:'Could not save settings.')}finally{setSaving(false)}}if(!data)return <div className="space-y-5"><PageHeader description="Restaurant identity, receipts, tax, loyalty and notifications."/><Card><CardContent className="h-80 animate-pulse bg-muted/30"/></Card></div>;const r=data.restaurant,s=data.settings;return <div className="space-y-5"><PageHeader description="Restaurant identity, receipts, tax, loyalty and notifications." actions={manage?<Button loading={saving} onClick={()=>void save()}><Save className="size-4"/>Save changes</Button>:undefined}/><Tabs defaultValue="restaurant"><TabsList><TabsTrigger value="restaurant"><Building2 className="size-4"/>Restaurant</TabsTrigger><TabsTrigger value="receipt"><Receipt className="size-4"/>Receipt & tax</TabsTrigger><TabsTrigger value="loyalty"><Gift className="size-4"/>Loyalty</TabsTrigger><TabsTrigger value="notifications"><Bell className="size-4"/>Notifications</TabsTrigger></TabsList><TabsContent value="restaurant"><Card><CardHeader><CardTitle>Restaurant information</CardTitle></CardHeader><CardContent className="grid gap-4 sm:grid-cols-2"><F l="Trading name"><Input disabled={!manage} value={r.name} onChange={e=>setData({...data,restaurant:{...r,name:e.target.value}})}/></F><F l="Legal name"><Input disabled={!manage} value={r.legalName??''} onChange={e=>setData({...data,restaurant:{...r,legalName:e.target.value}})}/></F><F l="Currency"><Select disabled={!manage} value={r.defaultCurrency} onValueChange={v=>setData({...data,restaurant:{...r,defaultCurrency:v}})}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{['USD','EUR','GBP','LKR','AED','INR'].map(x=><SelectItem key={x} value={x}>{x}</SelectItem>)}</SelectContent></Select></F><F l="Timezone"><Input disabled={!manage} value={r.timezone} onChange={e=>setData({...data,restaurant:{...r,timezone:e.target.value}})}/></F></CardContent></Card></TabsContent><TabsContent value="receipt"><Card><CardHeader><CardTitle>Receipt and charging rules</CardTitle></CardHeader><CardContent className="grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><F l="Receipt header"><Textarea disabled={!manage} value={s.receiptHeaderText} onChange={e=>setData({...data,settings:{...s,receiptHeaderText:e.target.value}})}/></F></div><div className="sm:col-span-2"><F l="Receipt footer"><Textarea disabled={!manage} value={s.receiptFooterText} onChange={e=>setData({...data,settings:{...s,receiptFooterText:e.target.value}})}/></F></div><F l="Paper size"><Select disabled={!manage} value={s.defaultReceiptPaperSize} onValueChange={v=>setData({...data,settings:{...s,defaultReceiptPaperSize:v}})}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="58mm">58 mm thermal</SelectItem><SelectItem value="80mm">80 mm thermal</SelectItem><SelectItem value="a4">A4 invoice</SelectItem></SelectContent></Select></F><F l="Service charge %"><Input disabled={!manage} type="number" value={s.serviceChargePercent} onChange={e=>setData({...data,settings:{...s,serviceChargePercent:Number(e.target.value)}})}/></F><F l="Order prefix"><Input disabled={!manage} value={s.orderNumberPrefix} onChange={e=>setData({...data,settings:{...s,orderNumberPrefix:e.target.value}})}/></F><F l="Invoice prefix"><Input disabled={!manage} value={s.invoiceNumberPrefix} onChange={e=>setData({...data,settings:{...s,invoiceNumberPrefix:e.target.value}})}/></F></CardContent></Card></TabsContent><TabsContent value="loyalty"><Card><CardHeader><CardTitle>Loyalty economics</CardTitle></CardHeader><CardContent className="grid gap-4 sm:grid-cols-2"><F l="Points per currency unit"><Input disabled={!manage} type="number" value={s.loyaltyPointsPerCurrencyUnit} onChange={e=>setData({...data,settings:{...s,loyaltyPointsPerCurrencyUnit:Number(e.target.value)}})}/></F><F l="Redemption value (minor units)"><Input disabled={!manage} type="number" value={s.loyaltyRedemptionRateMinorPerPoint} onChange={e=>setData({...data,settings:{...s,loyaltyRedemptionRateMinorPerPoint:Number(e.target.value)}})}/></F></CardContent></Card></TabsContent><TabsContent value="notifications"><Card><CardHeader><CardTitle>Operational alerts</CardTitle></CardHeader><CardContent className="space-y-4"><Toggle l="Low-stock notifications" d="Alert managers when ingredients cross their reorder level." checked={s.notifyLowStock} on={v=>setData({...data,settings:{...s,notifyLowStock:v}})} disabled={!manage}/><Toggle l="Reservation reminders" d="Surface upcoming guest bookings to the service team." checked={s.notifyReservationReminders} on={v=>setData({...data,settings:{...s,notifyReservationReminders:v}})} disabled={!manage}/></CardContent></Card></TabsContent></Tabs></div>}
function F({l,children}:{l:string;children:React.ReactNode}){return <div className="space-y-1.5"><Label>{l}</Label>{children}</div>}function Toggle({l,d,checked,on,disabled}:{l:string;d:string;checked:boolean;on:(v:boolean)=>void;disabled:boolean}){return <div className="flex items-center justify-between rounded-lg border p-4"><div><p className="text-sm font-medium">{l}</p><p className="text-xs text-muted-foreground">{d}</p></div><Switch checked={checked} onCheckedChange={on} disabled={disabled}/></div>}
